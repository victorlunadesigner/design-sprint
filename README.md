# Design Sprint

Async 4-week product design sprint — client proposal deck.

## Live page (GitHub Pages)

- [Presentation](https://victorlunadesigner.github.io/design-sprint/)
- Direct path: [proposal/](https://victorlunadesigner.github.io/design-sprint/proposal/)

Deployed from the `main` branch (repo root). A root `index.html` redirects to `proposal/`.

## Local preview

```bash
cd proposal && python3 -m http.server 8080
```

Then visit `http://localhost:8080/`.
